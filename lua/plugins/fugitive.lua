return {
  "tpope/vim-fugitive",
  config = function()
    -- Additional configuration can go here if needed.
    -- vim.cmd([[ nnoremap <leader>gs :Git<CR> ]]) -- Example keybinding for Git status
  end,
}
